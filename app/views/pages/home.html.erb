<main class="container">
  <h1>Quote editor</h1>
  <p>A balzing fast quote editor build with Hotwire</p>

  <% if user_signed_in? %>
    <%= link_to "View quotes", quotes_path, class: "btn brn-dark" %>
  <% else %>
    <%= link_to "Sign in", new_user_session_path, class: "btn btn-dark" %>
  <% end %>
</main>